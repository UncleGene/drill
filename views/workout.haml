Progress:
= student.progress
%br
- if student.on_plate
  %h1
    Next:
    = student.on_plate.name
  %form{:action => '/done', :method => :post}
    %input{:type => 'submit', :name => 'submit', :value => 'I have finished!'}
- else
  You do not have any exercises

